<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Responsive Web App</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <style>
    body {
        background-color: #1a1a1a;
        color: #fff;
        display: flex;
        flex-direction: column;
        min-height: 100vh;
    }

    .container {
        flex-grow: 1; /* Allow content to grow and fill space */
    }

    header {
        display: flex;
        position: sticky;
        justify-content: space-between;
        align-items: center;
        /* padding: px 30px; */
        padding-bottom: 20px;
    }

    /* Left side: Profile Image and Name */
    .user-info {
        display: flex;
        align-items: center;
    }

    .user-info img {
        width: 55px;
        height: 55px;
        border-radius: 50%;
        margin-right: 10px;
    }

    .user-info .name {
        font-size: 18px;
        font-weight: bold;
    }

    /* Right side: Round Buttons */
    .right-buttons {
        display: flex;
        gap: 15px; /* Space between buttons */
    }

    .right-btn {
        background-color: #333;
        color: white;
        border: none;
        font-size: 18px;
        width: 40px;
        height: 40px;
        border-radius: 50%; /* Make buttons round */
        display: flex;
        justify-content: center;
        align-items: center;
        cursor: pointer;
        transition: background-color 0.3s;
    }

    .right-btn:hover {
        background-color: #444;
    }

    /* Footer Section with Sticky Buttons */
    footer {
        background-color: transparent;
        /* box-shadow: 0 -2px 10px rgba(96, 79, 79, 0.5); */
        position: sticky;
        bottom: 0;
        left: 0;
        width: 100%;
        z-index: 10;
        display: flex;
        justify-content: space-between;
        margin: 0;
        padding: 0;
        margin-bottom: 30px;
    }

    footer .container {
        padding: 0;
    }

    .nav-btn {
        background-color: #333;
        color: #fff;
        border: none;
        font-size: 14px;
        padding: 12px 0;
        cursor: pointer;
        flex-grow: 1;
        transition: background-color 0.3s, transform 0.3s;
        backdrop-filter: blur(5px);
    }

    footer .nav-btn:first-child {
        border-radius: 30px 0 0 30px;
    }

    footer .nav-btn:last-child {
        border-radius: 0 30px 30px 0;
    }

    .nav-btn:hover {
        background-color: #444;
        
    }

    .nav-btn:focus {
        outline: none;
    }

    @media (max-width: 600px) {
        .container {
            padding: 15px;
        }

        footer {
            box-shadow: none;
        }

        .nav-btn {
            font-size: 20px;
        }

        .right-btn {
            font-size: 20px;
            width: 40px;
            height: 40px;
        }
    }
    .welcome-message {
    font-size: 16px;
    color: #ccc;
    margin-bottom: 5px; /* Add some space below the welcome message */
}

    </style>
    <style>
        /* General Styles for Calorie Goal Section */
.calorie-goal-section {
    background-color: #333;
    color: #fff;
    border-radius: 15px;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.5);
}

.circular-progress {
    position: relative;
    width: 120px; /* Default size */
    height: 120px; /* Default size */
    background: conic-gradient(#ff6b6b 70%, #444 70%);
    border-radius: 50%;
    display: flex;
    justify-content: center;
    align-items: center;
    margin: 0 auto;
}

.circular-progress .circle {
    width: 90%;
    height: 90%;
    background: #333;
    border-radius: 50%;
    display: flex;
    justify-content: center;
    align-items: center;
    color: #fff;
    flex-direction: column;
    text-align: center;
}

.circular-progress .remaining-calories span {
    font-size: 22px;
    font-weight: bold;
    color: #fff;
}

.circular-progress .remaining-calories small {
    font-size: 12px;
    color: #ccc;
}

.calorie-stats p {
    margin: 5px 0;
    font-size: 14px;
}

.calorie-stats i {
    margin-right: 8px;
    color: #ff6b6b;
}

.calorie-goal-section .small {
    margin-top: -5px;
    color: #999;
}

.scrollable-container {
    display: flex;
    gap: 16px;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    padding-bottom: 10px;
}

.scrollable-container > .calorie-goal-section {
    min-width: 100%;
    scroll-snap-align: start;
    background-color: #333;
    color: #fff;
    border-radius: 10px;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.5);
}

/* Dot Indicators */
.scroll-indicators {
    display: flex;
    justify-content: center;
    gap: 8px;
}

.scroll-indicators .dot {
    width: 10px;
    height: 10px;
    background-color: #444;
    border-radius: 50%;
    cursor: pointer;
    transition: background-color 0.3s;
}

.scroll-indicators .dot.active {
    background-color: #ff6b6b;
}

/* Mobile Responsive Design (Adjust the circular progress size for mobile) */
@media (max-width: 768px) {
    .circular-progress {
        width: 100px; /* Smaller size for mobile */
        height: 100px; /* Smaller size for mobile */
    }

    .circular-progress .circle {
        width: 85%; /* Adjust circle inside progress */
        height: 85%; /* Adjust circle inside progress */
    }

    .circular-progress .remaining-calories span {
        font-size: 18px; /* Smaller text inside the circle */
    }

    .circular-progress .remaining-calories small {
        font-size: 10px; /* Smaller text for the label */
    }

    /* Adjust gap in scrollable container for mobile */
    .scrollable-container {
        gap: 12px; /* Reduce gap between sections */
    }
}

    </style>
    <style>
        /* Progress Flow Chart Container */
.progress-flow-chart {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-top: 20px;
    padding: 20px;
}

/* Each Step in the Flow */
.progress-step {
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
}

/* Circle Representing Each Step */
.step-circle {
    width: 40px;
    height: 40px;
    background-color: #ff6b6b;
    border-radius: 50%;
    color: #fff;
    display: flex;
    justify-content: center;
    align-items: center;
    font-size: 16px;
    font-weight: bold;
    transition: background-color 0.3s;
}

/* Label under Each Step Circle */
.step-label {
    margin-top: 8px;
    font-size: 14px;
    color: #fff;
}

/* Line Connecting Each Step */
.step-line {
    width: 2px;
    height: 40px;
    background-color: #ff6b6b;
}

/* Active Step - Highlighted */
.step-circle.active {
    background-color: #4caf50; /* Active step color */
}

.step-label {
    color: #fff;
}

/* Responsive Design for Smaller Screens */
@media (max-width: 768px) {
    .progress-flow-chart {
        flex-direction: column;
        align-items: center;
    }
    .step-line {
        display: none; /* Remove lines for smaller screens */
    }
    .step-circle {
        width: 35px;
        height: 35px;
        font-size: 14px;
    }
}

    </style>
    <style>
        .progress-chart-container {
    width: 100%;
    max-width: 600px;
    margin: 0 auto;
    padding: 20px;
    background-color: #333;
    border-radius: 15px;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.5);
}

    </style>
</head>
<body>

    <div class="container py-4">
        <!-- Header Section -->
        <header>
            <!-- Left side: Profile Image and Name -->
            <div class="user-info">
                <img src="/assets/IMG_3032.JPG" alt="Profile" class="rounded-circle">
                <div class="name">
                    <div class="welcome-message">Welcome back!</div>
                    <strong>Salman</strong>
                </div>
            </div>
            <!-- Right side: Buttons -->
            <div class="right-buttons">
                <button class="right-btn"><i class="bi bi-chat"></i></button>
                <button class="right-btn"><i class="bi bi-bell"></i></button>
            </div>
        </header>
    
        <h4 style="text-align: left; font-size: 18px;">Today</h4>

        <!-- Horizontal Scrollable Container -->
        <div class="scrollable-container d-flex overflow-auto" id="scrollableContainer">
            <!-- Calorie Goal Section -->
            <div class="calorie-goal-section text-center p-4 rounded mx-2">
                <h4 style="text-align: left;">Calorie</h4>
                <p style="text-align: left; color: #999; font-size: 14px;">Remaining = Goal - Food + Exercise</p>
                <div class="calorie-details d-flex justify-content-between align-items-center mt-3">
                    <div class="circular-progress">
                        <div class="circle">
                            <div class="remaining-calories">
                                <span>3,000</span>
                                <small>Remaining</small>
                            </div>
                        </div>
                    </div>
                    <div class="calorie-stats text-start ms-3">
                        <p><i class="bi bi-flag"></i> Base Goal: <strong>3,000</strong></p>
                        <p><i class="bi bi-egg-fried"></i> Food: <strong>0</strong></p>
                        <p><i class="bi bi-fire"></i> Exercise: <strong>0</strong></p>
                    </div>
                </div>
            </div>
        
            <!-- Macronutrients Goal Section -->
            <div class="calorie-goal-section text-center p-4 rounded mx-2">
                <h4 style="text-align: left;">Macronutrients</h4>
                <p style="text-align: left; color: #999; font-size: 14px;">50% Carbohydrates, 30% Fat, 20% Protein</p>
                <div class="nutrient-details d-flex justify-content-around align-items-center mt-3">
                    <div class="circular-progress">
                        <div class="circle">
                            <div class="remaining-calories">
                                <span id="carbs-grams">0</span>
                                <small>g</small>
                            </div>
                            <small>Carb</small>
                        </div>
                    </div>
                    <div class="circular-progress">
                        <div class="circle">
                            <div class="remaining-calories">
                                <span id="fat-grams">0</span>
                                <small>g</small>
                            </div>
                            <small>Fat</small>
                        </div>
                    </div>
                    <div class="circular-progress">
                        <div class="circle">
                            <div class="remaining-calories">
                                <span id="protein-grams">0</span>
                                <small>g</small>
                            </div>
                            <small>Protein</small>
                        </div>
                    </div>
                </div>
            </div>
            </div>
            <!-- Dot Indicators -->
            <div class="scroll-indicators text-center mt-3" id="scrollIndicators">
                <span class="dot active"></span>
                <span class="dot"></span>
            </div>
            
            <h4 style="text-align: left; font-size: 18px; padding-top: 20px;">Progress</h4>
            
            <div class="container">
                <h4 style="text-align: left; font-size: 18px; padding-top: 20px;">Weight Progress</h4>
                <canvas id="weightProgressChart"></canvas>
            </div>
            

            

    
        
    <!-- Footer Section with Sticky Buttons -->
    <footer>
        <div class="container">
            <div class="d-flex justify-content-between">
                <button class="btn nav-btn flex-fill mx-1"><i class="bi bi-house"></i></button>
                <button class="btn nav-btn flex-fill mx-9"><i class="bi bi-pie-chart"></i></button>
                <button class="btn nav-btn flex-fill mx-1"><i class="bi bi-person"></i></button>
            </div>
        </div>
    </footer>
    
    <!-- Bootstrap JS and dependencies -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.min.js"></script>
    <script>
      // Prepare your data
const data = {
    labels: ['-11-01', '-11-02', '11-03', '-11-04', '-11-05'], // Dates
    datasets: [{
        label: 'Weight Progress (kg)',
        data: [75, 74.5, 74, 73.5, 73], // Weight corresponding to each date
        backgroundColor: 'rgba(75, 192, 192, 0.6)', // Bar color
        borderColor: 'rgba(75, 192, 192, 1)', // Border color
        borderWidth: 1
    }]
};

// Set up the bar chart
const ctx = document.getElementById('weightProgressChart').getContext('2d');
const weightProgressChart = new Chart(ctx, {
    type: 'line', // Bar chart
    data: data,
    options: {
        responsive: true,
        scales: {
            y: {
                beginAtZero: true, // Start Y-axis at 0
                title: {
                    display: true,
                    text: 'Weight (kg)'
                }
            },
            x: {
                title: {
                    display: true,
                    text: 'Date'
                }
            }
        },
        plugins: {
            legend: {
                display: false // Hide legend if you don't need it
            }
        }
    }
});


    </script>
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const container = document.getElementById('scrollableContainer');
            const dots = document.querySelectorAll('.scroll-indicators .dot');
    
            // Update active dot based on scroll position
            container.addEventListener('scroll', () => {
                const scrollPosition = container.scrollLeft;
                const containerWidth = container.scrollWidth / dots.length;
    
                dots.forEach((dot, index) => {
                    if (scrollPosition >= index * containerWidth && scrollPosition < (index + 1) * containerWidth) {
                        dot.classList.add('active');
                    } else {
                        dot.classList.remove('active');
                    }
                });
            });
    
            // Add click events to dots for navigation
            dots.forEach((dot, index) => {
                dot.addEventListener('click', () => {
                    const scrollTo = index * container.offsetWidth;
                    container.scrollTo({ left: scrollTo, behavior: 'smooth' });
                });
            });
        });
    </script>
    
</body>
</html>


